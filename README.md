# Agent Helper

**中文** | [English](README.en.md)

> 你舍不得骑的自行车，别人站起来蹬，agent helper，帮你站起来蹬！

Agent Helper是一个跑在本机的 Codex / ZCode 任务监控与排程助手。它盯着正在工作的任务，记住你想做的下一步，在额度恢复、前一项任务结束或指定时间到达时，替你把下一句 Prompt 送回正确的 Codex 对话。总览页同时统计 Codex 与 ZCode（只读导入 `~/.zcode/cli/db/` 的用量数据库）的模型使用、token 与错误。

简单说：你负责提出宏伟目标，Codex 负责写代码，Codex Helper 负责在大家都快忘了这件事的时候把工作接起来。

## 它主要解决什么问题

### 1. 任务结束了，下一步不用靠记忆

在“任务排程”选项卡中选择“发送下一步”，按 Codex 的 **项目 → 任务** 层级找到具体工作，填写下一步 Prompt。当前任务结束后，Helper 会向同一对话追加一轮。

如果任务已经结束，保存后立即执行；如果任务仍在运行，就等当前轮完成后再发送。任务选择器包含本机所有未归档的 work 任务，也包含较早结束的任务。

### 2. 额度用完了，别让工作躺平

> 白花花的token 额度，都散给了 sam altman，造孽啊！

“续跑任务”可以接住被额度限制打断的工作。Helper 会监控任务状态；额度恢复后，继续向原对话发送 Prompt。已结束的任务也可以手动选择并立即续跑。

还可以开启“额度恢复后自动续跑”：只监控开关打开之后发生的额度中断，避免把多年前的遗憾突然复活。

### 3. 新任务按计划启动

启动新任务时可以：

- 在指定时间运行；
- 下一次额度刷新后运行；
- 等另一项正在运行的任务结束后运行；
- 选择已有的本地 Codex 项目；
- 创建一个新项目目录并在其中运行。

排程队列会显示等待中、执行中和历史规则，等待中的规则可以取消。

- **总览**：额度水位、模型使用、错误和本地监测数据；
- **任务监控与排程**：任务状态、自动续跑开关、项目 → 任务选择器和排程队列；
- **任务选择器**：先选项目，再选具体对话，避免把项目名误当成任务；
- **历史规则**：查看已完成、失败、取消或需要检查的排程。

后台每 5 秒扫描一次本地任务状态。总览页从 Codex 账户接口读取实时额度，后台约每 30 秒更新一次；存在等待额度的排程时，查询间隔缩短到约 10 秒。总览页面每 30 秒刷新，并同时显示“剩余”和“已用”百分比及采样时间。点击面板右上角刷新按钮可以立即刷新当前页面。

## 安装

### macOS Apple Silicon

下载 [Agent Helper ARM64 DMG](https://github.com/ycheng-allen/agent-helper/releases/latest)，拖入“应用程序”后启动。当前构建未经过 Apple Developer ID 签名，首次打开时可在 Finder 中右键选择“打开”。

也可以从源码构建：

```bash
npm install
npm run dist
```

生成的安装包位于 `dist/Agent Helper-*.dmg`。

### 从源码运行

要求 Python 3.8+、Node.js 和本机已登录的 `codex` CLI：

```bash
git clone https://github.com/ycheng-allen/agent-helper.git
cd agent-helper
npm install
npm start
```

开发模式也可以直接运行本地服务：

```bash
python3 agent_helper.py --no-open
```

所有服务只监听 `127.0.0.1`。排程执行依赖本机的 Codex 登录态和 `codex` CLI，应用退出或电脑睡眠时不会偷偷补跑；重新启动后，未完成的执行规则会标记为“需检查”。

## 数据与隐私

任务状态来自本机 Codex 的会话索引和 rollout 日志，排程规则保存在：

```text
~/.agent-helper/state.db
```

数据目录已从旧的 `~/.codex-model-watch/` 更名为 `~/.agent-helper/`，首次运行会自动迁移，无需手动处理。

项目目录、Prompt 和任务标题不会上传到第三方。Helper 不提供云端队列，也不会替你把 Prompt 发给任何额外的服务；实际执行仍使用你当前登录的 Codex CLI。

## 监测功能从哪里来

Agent Helper 的总览、模型用量、额度窗口、容量错误和主动探针能力，继承并扩展自 [ysh1112/codex-model-watch](https://github.com/ysh1112/codex-model-watch)。感谢原作者 ysh1112 提供本地 Codex 会话日志解析、用量统计和模型探针的基础实现。

本项目在此基础上加入了 Electron 菜单栏应用、任务状态监控、项目 → 任务选择、额度恢复续跑、下一步 Prompt、一次性排程和可视化队列。原项目的监测能力仍然保留，只是现在终于有人负责在额度见底时提醒大家别继续硬蹭了。

## 监测能力

- 从本地会话日志统计模型、轮次、token、耗时和错误（Codex）；
- 从 ZCode 本机用量数据库（`~/.zcode/cli/db/db.sqlite`）只读增量导入模型、轮次、token、时长与错误；
- 总览页可按来源（Codex / ZCode）筛选，模型与错误表带来源标记；
- 显示 5 小时 / 7 天额度窗口（Codex 与 ZCode，ZCode 额度来自其账户用量接口）；
- 记录容量拒单和额度限制错误；
- 使用主动探针检查请求模型与实际派出模型是否一致（仅 Codex）。

### ZCode 任务排程（实验性）

排程已支持 ZCode：任务选择器会列出 ZCode 最近会话，可以创建"启动新任务"（指定时间 / 关联任务完成后）、"发送下一步"和"续跑"规则；续跑会等待 ZCode 实时额度窗口恢复（每 30 秒采样；额度不可读时退回每 5 分钟重试，最多 8 次）；新任务也支持"下一次额度刷新后"触发。

### 免费玩命蹬（ZCode 专属）

第三个标签页「玩命蹬」面向闲时免费额度场景：定义一个时间窗口（每日窗口如 00:00–08:00，支持跨午夜；或一次性窗口），排入最多 50 个任务——每个任务有独立的 Prompt，可单独编辑和排序。项目可选现有目录或新建项目（设定保存位置和文件夹名，首个任务启动时创建）。窗口开启后按设定并发执行——并行数 1 即串行，N（上限 6）为最多 N 个同时跑。窗口结束或手动停止时，在跑任务先 terminate、5 秒后强杀，剩余队列标记跳过；应用重启会自动恢复队列。

ZCode 无头 CLI 缺省没有可用模型（模型选择由桌面 App 把守）。Helper 会从本机 ZCode 凭证（`~/.zcode/v2/credentials.json`）解密 coding-plan API key，生成独立的 personal provider 配置写入 `~/.agent-helper/zcode-provider-config.json`（0600 权限），通过 `ZCODE_PERSONAL_PROVIDER_CONFIG_FILE` 环境变量注入 CLI——不改动 ZCode 自身的任何文件；API key 轮换时会在执行失败后自动刷新重试。可用 `ZCODE_BIN` 环境变量指向自定义 CLI 路径。

主动探针会消耗一条很小的 Codex 请求额度，只有你主动点击时才会发出请求。历史日志无法可靠还原过去发生的模型切换，因此探针结果只代表发起探测时的状态。

默认（`--agents auto`）按本机数据目录自动探测要监控的 agent；也可以用 `--agents codex` 或 `--agents codex,zcode` 显式指定。`--zcode-home` 可自定义 ZCode 主目录（默认 `~/.zcode`）。

## 已知边界

- ZCode 桌面端渲染已打开的会话用的是内存运行时：任何无头排程写入的轮次都会持久化到 ZCode 数据库，但要等桌面端重新加载该会话（如重启 ZCode、或重新打开一个未驻留内存的会话）后才会显示；执行本身与历史记录不受影响。

- 这是本机助手，不是云端任务队列；电脑关机、睡眠或应用退出时无法触发规则；
- Codex rollout 和状态数据库属于本地内部格式，未来 Codex 版本变化可能需要适配；
- 额度水位来自 Codex 上报数据，界面展示会有刷新间隔；
- ZCode 支持覆盖使用统计、实时额度与排程（实验性）：偷换探针仅支持 Codex；
- ZCode 导入只读访问 `~/.zcode/cli/db/db.sqlite`，不会写入 ZCode 的任何数据；
- 任务选择器排除已归档任务和子代理任务；
- 新项目会在规则真正触发时创建，避免排程还没执行目录就先占坑。

## License

本项目沿用 MIT License，并保留原始项目的来源说明。详见 [LICENSE](LICENSE)。
